build --bundles msi

# Optional if auto-detect fails (example SDK path):
# $env:SIGNTOOL_PATH = "C:\Program Files (x86)\Windows Kits\10\bin\10.0.19041.0\x64\signtool.exe"

cargo tauri build 

# full build includes pyinstaller exe
cargo tauri build --bundles msi --config tauri.fast.conf.json
# or
# skip pyinstaller build and only rebuild src-tauri main.rs
cargo tauri build --bundles msi --config '{\"build\": {\"beforeBuildCommand\": \"\"}}'
